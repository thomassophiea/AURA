# Kroger Sidebar Styling - Dec 29 2025 19:45 - FORCE REBUILD v6
[build]
builder = "nixpacks"

[build.nixpacksPlan]
phases.setup.nixPkgs = ["nodejs"]
phases.install.cmds = ["npm ci --no-cache"]
phases.build.cmds = ["rm -rf node_modules/.vite build dist && npm run build"]

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
