/**
 * Device Profiles (EPB-125 Configure port) — full 12-tab profile editor.
 * Public entry: the list page. Everything else is internal to this module.
 */
export { ProfilesPage } from './ProfilesPage';
